from django.apps import AppConfig


class KeelWebClientConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "keel_web.client"
    label = "keel_web_client"
    verbose_name = "Keel Web — Client panel"
