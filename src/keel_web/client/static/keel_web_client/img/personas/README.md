# Personas SVG layers

Sourced from [draftbit/avatar-generator](https://github.com/draftbit/avatar-generator)
("Personas by Draftbit"), MIT licensed. See `LICENSE`.

All layers share `viewBox="0 0 64 64"` and stack via `position: absolute`.
Composition order (bottom → top): Background → Body → Skin → Hair → FacialHair → Mouth → Nose → Eyes.
